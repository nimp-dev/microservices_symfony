name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-services:
    name: Build Services
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service: [order-service, user-service, notification-service]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build ${{ matrix.service }}
        run: |
          echo "🚧 Building image for ${{ matrix.service }}"
          docker build -t ${{ matrix.service }}:${{ github.sha }} ./services/${{ matrix.service }}

  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: build-services

    strategy:
      matrix:
        service: [order-service, user-service, notification-service]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and test ${{ matrix.service }}
        run: |
          echo "🚀 Building service: ${{ matrix.service }}"
          docker build -t ${{ matrix.service }} ./services/${{ matrix.service }}
          
          echo "🏃‍♂️ Running container..."
          docker run -d --rm -p 8000:8000 --name test-${{ matrix.service }} \
            ${{ matrix.service }} php -S 0.0.0.0:8000 -t public
          
          echo "⏳ Waiting for service to start..."
          for i in {1..10}; do
            if curl -fsS http://localhost:8000/health > /dev/null; then
              echo "✅ Service ${{ matrix.service }} is healthy!"
              break
            fi
            echo "⏳ Waiting... ($i/10)"
            sleep 2
          done
          
          # Проверка статуса после попыток
          if ! curl -fsS http://localhost:8000/health > /dev/null; then
            echo "❌ Health check failed for ${{ matrix.service }}"
            docker logs test-${{ matrix.service }}
            docker stop test-${{ matrix.service }} || true
            exit 1
          fi
          
          echo "✅ Service ${{ matrix.service }} passed health check."
          docker stop test-${{ matrix.service }} || true